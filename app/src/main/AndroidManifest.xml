<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cassistant.app">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_preferences"
        android:label="C助手"
        android:theme="@style/Theme.Material3.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FloatingBallService"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
文件7：app/src/main/java/com/cassistant/app/MainActivity.kt
package com.cassistant.app

import android.app.AlertDialog
import android.content.Intent
import android.net.Uri
import android.os.Bundle
import android.provider.Settings
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import androidx.core.content.ContextCompat
import com.cassistant.app.databinding.ActivityMainBinding

class MainActivity : AppCompatActivity() {
    
    private lateinit var binding: ActivityMainBinding
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
        
        initViews()
    }
    
    private fun initViews() {
        // 悬浮球开关
        binding.switchFloatingBall.setOnCheckedChangeListener { _, isChecked ->
            if (isChecked) {
                if (Settings.canDrawOverlays(this)) {
                    startFloatingBall()
                } else {
                    requestOverlayPermission()
                    binding.switchFloatingBall.isChecked = false
                }
            } else {
                stopService(Intent(this, FloatingBallService::class.java))
            }
        }
        
        // 桌面整理
        binding.btnOrganize.setOnClickListener {
            Toast.makeText(this, "桌面整理功能开发中", Toast.LENGTH_SHORT).show()
        }
        
        // 存储清理
        binding.btnClean.setOnClickListener {
            val cacheSize = getCacheSize()
            AlertDialog.Builder(this)
                .setTitle("存储清理")
                .setMessage("发现缓存: ${formatSize(cacheSize)}")
                .setPositiveButton("清理") { _, _ ->
                    clearCache()
                    Toast.makeText(this, "清理完成", Toast.LENGTH_SHORT).show()
                }
                .setNegativeButton("取消", null)
                .show()
        }
    }
    
    private fun startFloatingBall() {
        ContextCompat.startForegroundService(this, Intent(this, FloatingBallService::class.java))
        Toast.makeText(this, "悬浮球已开启", Toast.LENGTH_SHORT).show()
    }
    
    private fun requestOverlayPermission() {
        startActivity(Intent(Settings.ACTION_MANAGE_OVERLAY_PERMISSION,
            Uri.parse("package:$packageName")))
    }
    
    private fun getCacheSize(): Long {
        var size = 0L
        cacheDir?.let { size += it.walkTopDown().map { f -> if (f.isFile) f.length() else 0 }.sum() }
        externalCacheDir?.let { size += it.walkTopDown().map { f -> if (f.isFile) f.length() else 0 }.sum() }
        return size
    }
    
    private fun clearCache() {
        cacheDir?.deleteRecursively()
        externalCacheDir?.deleteRecursively()
    }
    
    private fun formatSize(size: Long): String {
        return when {
            size >= 1024 * 1024 * 1024 -> String.format("%.2f GB", size / (1024.0 * 1024.0 * 1024.0))
            size >= 1024 * 1024 -> String.format("%.2f MB", size / (1024.0 * 1024.0))
            size >= 1024 -> String.format("%.2f KB", size / 1024.0)
            else -> "$size B"
        }
    }
}
文件8：app/src/main/java/com/cassistant/app/FloatingBallService.kt
package com.cassistant.app

import android.app.Notification
import android.app.NotificationChannel
import android.app.NotificationManager
import android.app.PendingIntent
import android.app.Service
import android.content.Context
import android.content.Intent
import android.graphics.PixelFormat
import android.os.Build
import android.os.IBinder
import android.view.Gravity
import android.view.MotionEvent
import android.view.View
import android.view.WindowManager
import android.widget.ImageView
import android.widget.Toast
import androidx.core.app.NotificationCompat

class FloatingBallService : Service() {
    
    private lateinit var windowManager: WindowManager
    private lateinit var floatingView: ImageView
    
    override fun onCreate() {
        super.onCreate()
        windowManager = getSystemService(Context.WINDOW_SERVICE) as WindowManager
        createNotificationChannel()
        startForeground(1, createNotification())
        initFloatingBall()
    }
    
    override fun onBind(intent: Intent?): IBinder? = null
    
    private fun createNotificationChannel() {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            val channel = NotificationChannel("cassistant", "C助手", NotificationManager.IMPORTANCE_LOW)
            getSystemService(NotificationManager::class.java).createNotificationChannel(channel)
        }
    }
    
    private fun createNotification(): Notification {
        val intent = Intent(this, MainActivity::class.java)
        val pendingIntent = PendingIntent.getActivity(this, 0, intent, PendingIntent.FLAG_IMMUTABLE)
        return NotificationCompat.Builder(this, "cassistant")
            .setContentTitle("C助手运行中")
            .setContentText("悬浮球已开启")
            .setSmallIcon(android.R.drawable.ic_menu_preferences)
            .setContentIntent(pendingIntent)
            .setOngoing(true)
            .build()
    }
    
    private fun initFloatingBall() {
        floatingView = ImageView(this).apply {
            setImageResource(android.R.drawable.ic_menu_preferences)
            setBackgroundResource(android.R.drawable.circle_mask)
            setPadding(20, 20, 20, 20)
        }
        
        val params = WindowManager.LayoutParams(
            120, 120,
            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) 
                WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY
            else 
                WindowManager.LayoutParams.TYPE_PHONE,
            WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,
            PixelFormat.TRANSLUCENT
        ).apply {
            gravity = Gravity.TOP or Gravity.START
            x = 50
            y = 400
        }
        
        var initialX = 0
        var initialY = 0
        var touchX = 0f
        var touchY = 0f
        
        floatingView.setOnTouchListener { _, event ->
            when (event.action) {
                MotionEvent.ACTION_DOWN -> {
                    initialX = params.x
                    initialY = params.y
                    touchX = event.rawX
                    touchY = event.rawY
                    true
                }
                MotionEvent.ACTION_MOVE -> {
                    params.x = initialX + (event.rawX - touchX).toInt()
                    params.y = initialY + (event.rawY - touchY).toInt()
                    windowManager.updateViewLayout(floatingView, params)
                    true
                }
                MotionEvent.ACTION_UP -> {
                    if (kotlin.math.abs(event.rawX - touchX) < 10) {
                        Toast.makeText(this, "C助手菜单", Toast.LENGTH_SHORT).show()
                    }
                    true
                }
                else -> false
            }
        }
        
        windowManager.addView(floatingView, params)
    }
    
    override fun onDestroy() {
     
...(truncated)...
